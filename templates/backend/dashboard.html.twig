{% extends "@Contao/be_main" %}
    
    {% block main_content %}
        <div class="tl_listing_container">
            <h1>{{ headline }}</h1>
            {% if setupComplete %}
                <p class="tl_confirm">Die Pflichtkonfiguration ist vollständig.</p>
            {% else %}
                <p class="tl_error">Die Pflichtkonfiguration ist unvollständig.</p>
            {% endif %}
        </div>
    {% endblock %}
